(function ($) {

/* Execute code when the DOM is fully loaded */
$(document).ready(function() {


  //warningCNIL();

  //managePopins();

  //managePolyfills();

/*
  // Sliding Panel
  slidingPanel({
    overlay: false,
    wrapperSlide: false,
    position: 'left',
  });
*/
/*
  // Pop login
  popPanel({
    trigger: $('#pop-login-trigger'),
    idPopTitle: 'pop-login-title',
    position: {
      my: 'right top',
      at: 'right+10 bottom',
      collision: 'window',
      within : '.wrapper'
    }
  });
*/
/*
  // Pop search filter
  popPanel({
    trigger: $('#form-choices-trigger'),
    position: {
      my: 'left top',
      at: 'left bottom',
      within : '.wrapper'
    }
  });
*/

  // Collapsible menu
/*
  // Close all
  $('div.nav-menu .expanded').toggleClass('expanded collapsed').find('> ul').hide();

  collapsibleList({
    list: $('div.nav-menu ul')
  });
*/

}); // /ready


})(jQuery);
